document.addEventListener('DOMContentLoaded', function () {
    // Background loader disabled to keep black background
    console.log('Background loader disabled to maintain pitch black background');
});
